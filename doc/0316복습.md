CMake --> makefile 자동생성해주는 유틸리티
 - 문법         
 - vscode 설정
 => 같이 사용

분할 컴파일 
 -> 기능별로 파일을 관리
 -> 코드의 재사용
        => 라이브러리의 배포

```
  main.c   main.o  => 기능별로 파일을 관리
    a.h
    a.c     a.o     -> (a.h + a.o) |
    b.h                            | => 실행파일
    b.c     b.o     -> (b.h + b.o) |       
 ```

```c
#ifdef  => #progma once
#define
    |
#endif  
```

전역변수는 보안 이슈로 사용을 잘 하지 않는다

`static` 정적변수 -> 전역변수처럼 이용할 수 있다 `함수 안에서만 전역변수로 쓰인다`
함수를 여러번 call을 할 때 메모리를 유지


